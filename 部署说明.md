# IndexTTS-2 一键部署说明（N卡用户）

## 📦 快速开始

### 方法一：Conda 版本（推荐新手）

**适用场景**：已安装 Anaconda/Miniconda 的用户

**步骤**：
1. 双击运行 `deploy_conda.bat`
2. **完全自动**，无需任何输入，等待完成即可
3. 运行 `start_webui.bat` 启动 WebUI

**自动化特性**：
- ✅ 自动检测环境（已存在则跳过创建）
- ✅ 自动使用 ModelScope 下载（失败自动切换 HuggingFace）
- ✅ 全程无需手动输入

**优点**：
- ✅ 简单易用，适合新手
- ✅ 不需要额外安装工具
- ✅ 与现有 conda 环境兼容
- ✅ 完全自动化部署

**缺点**：
- ⚠️ 非官方推荐方式
- ⚠️ 可能存在依赖版本冲突

---

### 方法二：UV 版本（官方推荐）

**适用场景**：追求稳定性和性能的用户

**步骤**：
1. 双击运行 `deploy_uv.bat`
2. **完全自动**，无需任何输入，等待完成即可
3. 运行 `start_webui_uv.bat` 启动 WebUI

**自动化特性**：
- ✅ 自动安装 uv 工具（如未安装）
- ✅ 自动使用 ModelScope 下载（失败自动切换 HuggingFace）
- ✅ 全程无需手动输入

**优点**：
- ✅ 官方推荐，稳定性最佳
- ✅ 依赖管理精准，避免版本冲突
- ✅ 安装速度更快（比 pip 快 115 倍）
- ✅ 完全自动化部署

**缺点**：
- ⚠️ 需要额外学习 uv 工具（可选）

---

## 🔧 前置要求

### 必需软件
- [x] **Git**：https://git-scm.com/downloads
- [x] **Git LFS**：https://git-lfs.com/
- [x] **CUDA Toolkit 12.1+**：https://developer.nvidia.com/cuda-toolkit
- [x] **Conda** (方法一) 或 **Python 3.10+** (方法二)

### 硬件要求
- **GPU**：NVIDIA 显卡（支持 CUDA）
- **显存**：建议 8GB+ (推荐 12GB+)
- **内存**：建议 16GB+
- **磁盘**：至少 20GB 可用空间

---

## 📥 模型下载

**部署脚本会自动处理模型下载**，无需手动选择：

### 自动下载策略
1. **优先使用 ModelScope**（国内用户推荐，速度快）
2. 如果失败，**自动切换到 HuggingFace**
3. 两者都失败时，提示手动下载

### 手动下载（可选）
如果自动下载失败，可以手动下载后放到 `checkpoints` 目录：
- **HuggingFace**：https://huggingface.co/IndexTeam/IndexTTS-2
- **ModelScope**：https://modelscope.cn/models/IndexTeam/IndexTTS-2

---

## ❓ 常见问题

### Q1: 部署时提示"未检测到 conda"
**A**: 需要先安装 Miniconda：https://docs.conda.io/en/latest/miniconda.html

### Q2: 模型下载失败
**A**:
- 检查网络连接
- 国内用户选择 ModelScope
- 可手动下载后放入 `checkpoints` 目录

### Q3: 启动时提示 CUDA 不可用
**A**:
1. 检查是否安装了 NVIDIA 驱动
2. 检查是否安装了 CUDA Toolkit 12.1+
3. 运行测试：`python -c "import torch; print(torch.cuda.is_available())"`

### Q4: 加载音频文件失败（m4a 格式）
**A**: 需要安装 ffmpeg（部署脚本已自动处理）

### Q5: 首次运行很慢
**A**: 正常现象，首次运行会自动下载一些小模型（campplus、semantic_codec 等），请耐心等待

---

## 🚀 使用方法

### 启动 WebUI
```bash
# Conda 版本
start_webui.bat

# UV 版本
start_webui_uv.bat
```

### 手动启动（高级用户）

**Conda 版本**：
```bash
conda activate IndexTTS
python webui.py
```

**UV 版本**：
```bash
uv run python webui.py
```

---

## 📝 国内网络优化

### 设置 HuggingFace 镜像
```bash
# Windows (临时)
set HF_ENDPOINT=https://hf-mirror.com

# Windows (永久)
setx HF_ENDPOINT "https://hf-mirror.com"
```

### UV 使用国内 PyPI 镜像
```bash
uv sync --all-extras --default-index "https://mirrors.aliyun.com/pypi/simple"
```

---

## 📧 技术支持

- **GitHub Issue**：https://github.com/index-tts/index-tts/issues
- **QQ 群**：663272642 (No.4) / 1013410623 (No.5)
- **Discord**：https://discord.gg/uT32E7KDmy
- **Email**：indexspeech@bilibili.com

---

## 📜 许可证

本项目遵循 Bilibili IndexTTS 许可协议。详见 `LICENSE` 文件。
